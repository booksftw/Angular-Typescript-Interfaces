export namespace ourInterfaces {
    export interface Customer {
        id: number;
        name: string;
        age: number;
    }

    export interface Animal {
        id: number;
        name: string;
        hungerLevel: number;
    }
}